# Build Configuration for LimeSurvey
# This file defines the Docker image configuration for building LimeSurvey

# Application image name (used for tagging in registry)
APP_IMAGE="limesurvey"

# Database image name (used for tagging in registry)
MARIADB_IMAGE="limesurvey-mariadb"

# Human-readable database name (used in display messages)
MARIADB_NAME="MariaDB"

