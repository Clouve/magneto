# Build Configuration for AI Studio (blank Ubuntu 26.04 LTS workspace, operated by Magneto Agent)
# This file defines the Docker image configuration for building AI Studio.
# ai-studio has no sub-image (no database sidecar), so only APP_IMAGE is set.

# Application image name (used for tagging in registry)
APP_IMAGE="ai-studio"
