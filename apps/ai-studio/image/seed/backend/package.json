{
  "name": "starter-backend",
  "private": true,
  "type": "module",
  "description": "Express + Mongoose API for the AI Studio MERN starter",
  "scripts": {
    "dev": "node --env-file=.env --watch src/server.js",
    "start": "node --env-file=.env src/server.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "mongoose": "^9.7.0"
  }
}
