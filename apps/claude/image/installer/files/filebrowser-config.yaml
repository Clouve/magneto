server:
  port: 8080
  listen: "127.0.0.1"
  baseURL: "/"
  sources:
    - path: "${HOME}"
      config:
        defaultEnabled: true

database: "/opt/filebrowser/database.db"

auth:
  methods:
    proxy:
      enabled: true
      header: "X-Forwarded-User"
      createUser: true
