# Build Configuration for ownCloud
# This file defines the Docker image configuration for building ownCloud

# Application image name (used for tagging in registry)
APP_IMAGE="owncloud"

# Database image name (used for tagging in registry)
MARIADB_IMAGE="owncloud-mariadb"

# Human-readable database name (used in display messages)
MARIADB_NAME="MariaDB"

# Redis cache image name (used for tagging in registry)
REDIS_IMAGE="owncloud-redis"

# Human-readable Redis name (used in display messages)
REDIS_NAME="Redis"

