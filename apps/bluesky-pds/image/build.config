# Build Configuration for Bluesky PDS (Personal Data Server)
# This file defines the Docker image configuration for building Bluesky PDS

# Application image name (used for tagging in registry)
APP_IMAGE="bluesky-pds"

# Note: Bluesky PDS does not require a separate database image
# It uses SQLite for data storage within the container


